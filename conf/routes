# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /json                       controllers.Application.json

GET     /login                      controllers.Login.login
GET     /loginAction                controllers.Login.loginAction
GET     /oauth2callback             controllers.Login.oauth2Callback
GET     /logout                     controllers.Login.logout
GET     /userinfo                   controllers.Login.userInfo

#GET     /api/queues                controllers.ModerationQueues.list
#GET     /api/queues/:queue/next    controllers.ModerationQueues.next(queue: String, discussionKey: Option[String])

# Map static resources from the /public folder to the /assets URL path
#GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET     /*file                      controllers.Assets.versioned(path="/public", file: Asset)